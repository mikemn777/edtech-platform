# Backend-local example for Prisma CLI (dev only). Real config lives in root .env
# and is injected from the secrets manager in staging/prod (Blueprint §11).
DATABASE_URL=postgresql://edu_app:change_me_in_local_only@localhost:5432/edu_platform?schema=public
